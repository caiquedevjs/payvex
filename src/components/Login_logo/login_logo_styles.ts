import { StyleSheet } from "react-native"

export const login_logo_styles = StyleSheet.create({
    Login_logo : {
        width : 350,
        height : 350,
        justifyContent : "center",
        alignItems : "center"
    }
})