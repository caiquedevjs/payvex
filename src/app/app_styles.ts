import { StyleSheet } from "react-native";

export  const app_styles = StyleSheet.create({

    App : {
       justifyContent : "center",
       alignItems: "center",
       flex: 1,
       padding: 32,
       gap : 10,
    }
})